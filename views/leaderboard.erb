<head>
    <title>Leaderboard</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
<h3> Leaderboard </h3>
<ol>
  <% @scores.each do |score| %>
    <li><a href="/score/<%= score[:home_team] %>"> <%= "#{score[:home_team]}" %></a></li>
  <% end %>
</ol>
